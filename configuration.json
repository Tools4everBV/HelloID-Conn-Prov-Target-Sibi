[
    {
        "key": "BaseUrl",
        "type": "input",
        "defaultValue": "",
        "templateOptions": {
            "label": "BaseUrl",
            "description": "The URL to the API.",
            "required": true
        }
    },
    {
        "key": "Token",
        "type": "input",
        "defaultValue": "",
        "templateOptions": {
            "type": "password",
            "label": "Token",
            "description": "The token needed to authenticate to the API.",
            "required": true
        }
    },
    {
        "key": "updateUserOnCorrelate",
        "type": "checkbox",
        "defaultValue": false,
        "templateOptions": {
            "label": "Update User when correlating and mapped data differs from data in Sibi",
            "required": false,
            "description": "This will update Sibi users in the create action (not just correlate)."
        }
    },
    {
        "key": "IsDebug",
        "type": "checkbox",
        "defaultValue": true,
        "templateOptions": {
            "label": "Toggle debug logging",
            "description": "When toggled, debug logging will be displayed",
            "required": false
        }
    }
]